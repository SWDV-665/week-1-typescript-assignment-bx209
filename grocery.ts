class Grocery {
    name: string;
    id: number;
    quantity: number;

    constructor(n: string, i: number, q: number) {
        this.name = n;
        this.id = i;
        this.quantity = q;
    }
}